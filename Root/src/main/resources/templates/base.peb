<!doctype html>
<html lang="en">
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>{{ title ?: "COMP2850 Library" }}</title>
    <link rel="stylesheet" href="/static/style.css" />
</head>

<body>
<header class="topbar">
    <div class="container topbar__inner">
        <a class="brand" href="/">
            <span class="brand__logo">📚</span>
            <span class="brand__text">{{ libraryName ?: "COMP2850 Library" }}</span>
        </a>

        <nav class="nav">
            <a class="nav__link" href="/">Home</a>
            {% if userId %}
                <a class="nav__link" href="/loans">My Loans</a>
                <a class="btn btn--ghost" href="/logout">Logout</a>
            {% else %}
                <a class="btn btn--ghost" href="/login">Sign in</a>
                <a class="btn btn--primary" href="/signup">Sign up</a>
            {% endif %}
        </nav>
    </div>
</header>

<main class="container">
    {% block content %}{% endblock %}
</main>

<footer class="footer">
    <div class="container footer__inner">
        <div>
            <div class="footer__title">COMP2850 Library</div>
            <div class="footer__muted">Simple demo system (Ktor + Pebble + SQLite)</div>
        </div>
        <div class="footer__muted">© {{ "now" | date("yyyy") }}</div>
    </div>
</footer>
</body>
</html>